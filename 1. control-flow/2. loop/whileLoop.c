#include <stdio.h>

int main() {
    int count = 1;

    // Print numbers from 1 to 5 using a while loop
    while (count <= 5) {
        printf("Count: %d\n", count);
        count++;
    }

    return 0;
}